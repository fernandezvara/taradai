es:
  errors:
    messages:
      invalid_date: "no es una fecha válida"
      invalid_time: "no es una hora válida"
      invalid_datetime: "no es una fecha/hora válida"
      is_at: "debe ser %{restriction}"
      before: "debe ser antes de %{restriction}"
      on_or_before: "deber ser antes de o el %{restriction}"
      after: "debe ser una fecha en el futuro"
      on_or_after: "debe ser después o el %{restriction}"
  validates_timeliness:
    error_value_formats:
      date: '%d-%m-%Y'
      time: '%H:%M:%S'
      datetime: '%d-%m-%Y %H:%M:%S'
